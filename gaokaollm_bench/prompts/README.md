# prompts

This package stores prompt builders only.

It does not call models or validate JSON. Its job is to turn typed inputs into stable prompts or chat messages that can be reused by chains, flows, simulator, evaluator, and CLI entrypoints.
