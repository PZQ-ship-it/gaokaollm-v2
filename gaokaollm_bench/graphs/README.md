# graphs

This package contains LangGraph pipeline builders.

It is the place for reusable graph wiring only. Prompt text lives in `prompts/`, provider access lives in `llm/`, and business orchestration lives in `flows/`.

