"""Shared constants and enum values for gaokaollm_bench.

The directory name follows the project request. New constants that are reused
across modules should live here rather than being copied into scripts.
"""
