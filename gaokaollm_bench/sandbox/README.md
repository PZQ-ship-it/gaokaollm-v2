# sandbox

This package runs controlled benchmark episodes.

The sandbox connects a target agent to a simulated user, records turns, stops on persuasion or max-turn limits, and persists transcripts. Target systems should enter through the abstract base agent interface.

